{
  "name": "@chevrotain/benchmark",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "bun run clean && bun run --filter chevrotain quick-build && node --experimental-strip-types src/run.ts",
    "clean": "rimraf results downloads"
  },
  "dependencies": {
    "chevrotain": "workspace:*",
    "xregexp": "5.1.1"
  }
}
