export { PRINT_WARNING, PRINT_ERROR } from "./print"
export { timer } from "./timer"
export { toFastProperties } from "./to-fast-properties"
export {
  has,
  includes,
  shallowClone,
  every,
  some,
  compact,
  difference,
  filter,
  flatten,
  forEach,
  map,
  mapObj,
  reduce,
  reject,
  uniq,
  flatMap,
  find
} from "./library"

// import \{ (compact|difference|filter|flatten|forEach|map|mapObj|reduce|reject|uniq|flatMap|find) \} from ".+"
