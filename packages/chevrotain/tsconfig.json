{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "lib",
    "baseUrl": ".",
    // raises too many errors to fix at once.
    "strict": false
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "api.d.ts"]
}
