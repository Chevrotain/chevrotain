{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "lib",
    "baseUrl": ".",
    // raises too many errors to fix at once.
    "strict": false,
    "skipLibCheck": true,
    "noImplicitAny": true,
    "strictFunctionTypes": true
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "api.d.ts"]
}
