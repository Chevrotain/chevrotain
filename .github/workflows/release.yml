name: Release
on:
  push:
    tags:
      - "v*"

permissions:
  id-token: write # Required for OIDC (npmjs trusted publishers)
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      # pnpm must be installed before setup-node so the cache key can be computed
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm install
      - name: Build
        run: pnpm run ci
      - name: Publish to NPM
        run: pnpm run release:publish
