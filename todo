
BUILD/RELEASE/HIGH PRIORITY


* finish readme.md

* bower release.

* npm release.

BLOCKED:
* travis integration for CI (needs to be on github first)


FEATURES:
* DSL for lookahead
* lookahead validations
* semantic validations on grammar (no duplicate CONSUME1 for example)
* in recovery parser RULE DSL to accept impel as the last argument thus allows in place definition?

  RULE(......, ....., ......, () => {
    CONSUME(...)
    MANY(...)
    ...
  }

* fault tolerance improvements for in repetitonRecovery? see example in spec called
 can also sometimes fail in automatic error recovery :)

EXAMPLES:
* html with live diagrams
* calculator


